# Design System (starter)

## Layout
- Container: max-w 1200px; gutters 24/32/40 (mobile/tablet/desktop)
- Section vertical rhythm: 64 / 96 / 128 (mobile/tablet/desktop)
- Baseline grid: 4px

## Spacing scale (px)
4, 8, 12, 16, 24, 32, 40, 48, 64, 80, 96, 128

## Type scale (clamp)
- Display: clamp(32px, 5vw, 56px)
- H1: clamp(28px, 4vw, 44px)
- H2: clamp(22px, 3vw, 32px)
- H3: clamp(18px, 2.4vw, 24px)
- Body: clamp(16px, 1.6vw, 18px); lh 1.7
- Headings lh 1.15–1.2

## Motion
- Enter 200ms fade/slide-up; hover 150ms
- No layout shift; respect prefers-reduced-motion

## Tokens created outside scale
- (Document here when Cursor adds named one-off tokens)
